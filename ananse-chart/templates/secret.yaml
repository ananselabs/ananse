apiVersion: v1
kind: Secret
metadata:
  name: ananse-webhook-certs
  namespace: {{.Values.namespace }}
type: kubernetes.io/tls
data:
  tls.crt: |
    {{.Values.certs }}
  tls.key: |
      {{.Values.key }}